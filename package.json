{
  "name": "cypress-mochawesome-docker",
  "version": "1.0.0",
  "description": "# UI Automation Framework",
  "main": "index.js",
  "dependencies": {
    "cypress-commands": "^1.1.0",
    "eslint": "^6.8.0",
    "eslint-plugin-cypress": "^2.10.3",
    "mocha": "^7.2.0",
    "mochawesome": "^6.2.1",
    "mochawesome-merge": "^4.0.3"
  },
  "devDependencies": {
    "cypress": "5.6.0"
  },
  "scripts": {
    "start": "npx cypress open",
    "test": "npx cypress run test --reporter mochawesome",
    "record": "npx cypress run test --record",
    "cy:sanity:prod:electron": "yarn run test --browser electron",
    "cy:sanity:prod:chrome": "yarn run test --browser chrome",
    "cy:sanity:prod:chrome:headless": "yarn run test --browser chrome",
    "cy:sanity:prod:chrome:headless:record": "yarn run test --browser chrome --headless --record --key ${CYPRESS_KEY}",
    "cy:sanity:prod:ff": "yarn run test --browser firefox",
    "cy:sanity:prod:edge": "yarn run test --browser edge"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eliran-shani/cypress-mochawesome-docker.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/eliran-shani/cypress-mochawesome-docker/issues"
  },
  "homepage": "https://github.com/eliran-shani/cypress-mochawesome-docker#readme"
}
